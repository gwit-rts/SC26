# `ptp4l` related files

## `ptp4l.conf`
located in `/etc/`

## `phc2sys`
No changes from the default config installed with the package.  
Located in `/etc/sysconfig/`

## `timemaster.conf`
Located in `/etc/`
